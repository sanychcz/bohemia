<div class="row">
  <%= simple_form_for @category do |f| %>
    <%= render 'shared/error_message', object: f.object %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.submit "Save", :class => "button" %> or <%= link_to "go back", :back %>
  <% end %>
</div>